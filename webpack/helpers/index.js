const { babelOptions } = require('./babelOptions');
const { jsLoaders } = require('./jsLoader');
const { styleLoader } = require('./styleLoader');
const { filename } = require('./filename');

module.exports = { babelOptions, jsLoaders, styleLoader, filename };